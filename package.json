{
  "name": "casa",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
